# ─────────────────────────────────────────────────────────────────────────────
# Nifty Hi-LSTM v2 — Python Dependencies
# Install with:  pip install -r requirements.txt
# Python ≥ 3.10 recommended
# ─────────────────────────────────────────────────────────────────────────────

# ── Core ML ──────────────────────────────────────────────────────────────────
torch>=2.1.0                    # Primary deep-learning framework (CPU / CUDA / MPS)
numpy>=1.24.0                   # Numerical arrays
pandas>=2.0.0                   # DataFrame operations
scikit-learn>=1.3.0             # Pre-processing, metrics, HMM regime fitting

# ── Technical Analysis ────────────────────────────────────────────────────────
# pandas-ta ships 130+ indicators and wraps TA-Lib without the C dependency
pandas-ta>=0.3.14b0
# ta is a lighter alternative/fallback with pure-pandas implementations
ta>=0.11.0

# ── Data Sources ──────────────────────────────────────────────────────────────
yfinance>=0.2.36                # Yahoo Finance: OHLCV, VIX, options chains
requests>=2.31.0                # HTTP requests (FII data, NSE web scraping)
feedparser>=6.0.10              # RSS news parsing
beautifulsoup4>=4.12.0          # HTML parsing for NSE / RBI pages
lxml>=4.9.3                     # Fast XML/HTML parser (used by bs4 + feedparser)

# ── NLP / Sentiment ───────────────────────────────────────────────────────────
transformers>=4.35.0            # HuggingFace: FinBERT sentiment model
# sentencepiece is required by some HuggingFace tokenisers
sentencepiece>=0.1.99
# VADER provides a fast rule-based fallback when FinBERT is unavailable
vaderSentiment>=3.3.2

# ── Scheduling / Live Predictor ───────────────────────────────────────────────
schedule>=1.2.1                 # Simple recurring job scheduler
pytz>=2023.3                    # Timezone handling (Asia/Kolkata)
apscheduler>=3.10.4             # Advanced alternative scheduler (optional)

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.7.0               # Equity curves, training loss plots
seaborn>=0.12.0                 # Statistical visualisations
plotly>=5.17.0                  # Interactive charts (optional dashboard)

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.66.0                    # Progress bars in training loops
python-dotenv>=1.0.0            # Load API keys from .env file
joblib>=1.3.0                   # Parallel processing, model persistence
click>=8.1.7                    # CLI utilities (optional future use)

# ── Optional / Enhanced Features ──────────────────────────────────────────────
# Uncomment these if you want GPU acceleration or advanced options

# nsepy>=0.8                    # NSE official Python library (historical data)
#                               # Note: nsepy may require older pandas; test first
# torch-audio                   # Only needed for audio tokeniser support
# accelerate>=0.24.0            # HuggingFace Accelerate for multi-GPU training
# optuna>=3.4.0                 # Hyperparameter optimisation
# mlflow>=2.8.0                 # Experiment tracking

# ── Development ───────────────────────────────────────────────────────────────
# pip install -r requirements-dev.txt for testing and linting deps
# pytest>=7.4.0
# pytest-cov>=4.1.0
# black>=23.0.0
# isort>=5.12.0
# mypy>=1.6.0
